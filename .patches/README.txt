# Patches for sibling orthodox-ronl repos

This cloud agent can push only to heiligen-lage-landen (cursor[bot] 403 on the others).

Apply on a clone of each repo, from that repo root:

    git am --3way <path-to-this-file>

Files: bron.patch, VSA-tooling.patch, VSA-demo.patch, koor.patch
Branch name used: cursor/org-scripts-conventie-e9f7
